extends layout

block contenido
    .contenido-principal
        h1 #{pageName} - #{project.name}



        //- pre= vardump(project)

        .contenedor-acciones
            a(href=`/project/edit/${project.id}` class="boton") Edit Project
            button#eliminar-proyecto(type="button" class="boton eliminar") Delete Project


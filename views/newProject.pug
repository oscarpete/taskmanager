extends layout

block contenido 
    .contenido-principal 
        h1 #{pageName}

        if errors 
            each error in errors
                .alerta= error.text

        form.agregar-proyecto(action="/newProject" method="POST")
            .campo 
                label(for="name") Project Name:
                input(type="text" id="name" name="name" placeholder="Name your Project")
            .campo.enviar 
                input(type="submit" value="Add" class="boton")

        